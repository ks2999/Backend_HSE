# ДЗ семинара 1 — консольный калькулятор в Docker.
# Явное имя проекта, чтобы не пересекаться с другими ДЗ (папки тоже зовутся homework).
name: seminar1-hw

services:
  app:
    build: ./app
    container_name: hw1-app
    # пример выражения; поменяйте на своё, например ["factorial", "5"]
    command: ["5", "+", "3"]
