# ДЗ семинара 6 — своя форма авторизации, выдающая JWT вместо JSESSIONID.
# Внешних хранилищ нет: пользователи заданы в памяти, поэтому сервис один — само приложение.
# Явное имя проекта, чтобы не пересекаться с другими ДЗ (папки тоже зовутся homework).
name: seminar6-hw

services:
  app:
    build: .
    container_name: hw6-app
    ports:
      - "8080:8080"
